import styled from "@emotion/styled";

export const Title = styled.h1`
  width: 100%;
  height: 40px;
  margin: 0;
  padding: 5px 0px;
  text-align: center;
  font-weight: normal;
  font-size: ${({theme}) => theme.font.big};
  line-height: 32px;
`;